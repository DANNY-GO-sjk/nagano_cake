<table class="table table-bordered">
  <thead>
    <th>ジャンル検索</th>
  </thead>
  <tbody>

    <% @genres.each do |genre| %>
      <tr>
        <td>
          <%= link_to genre.name, genres_search_path(genre) %>
        </td>
      </tr>
    <% end %>

  </tbody>
</table>