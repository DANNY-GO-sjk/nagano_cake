<table class="table table-bordered">
  <thead>
    <th>ジャンル検索</th>
  </thead>
  <tbody>

    <% @genres.each do |genre| %>
      <tr>
        <td>
          <%= link_to genre.name, genre_items_path(genre.id) %>
        </td>
      </tr>
    <% end %>

  </tbody>
</table>